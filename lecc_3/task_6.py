# Пользователь вводит строку текста. Вывести каждое слово с новой строки.
# ✔ Строки нумеруются начиная с единицы.
# ✔ Слова выводятся отсортированными согласно кодировки Unicode.
# ✔ Текст выравнивается по правому краю так, чтобы у самого длинного 
# слова был один пробел между ним и номером строки.


text = input("Введите строку текста: ")

# Разделяем строку на слова
words = text.split()

# Сортируем слова по кодировке Unicode
sorted_words = sorted(words)

# Находим максимальную длину слова
max_length = max(len(word) for word in sorted_words)

# Выводим слова с номерами строк, выравнивая по правому краю
for index, word in enumerate(sorted_words, start=1):
    print(f"{index:>2} {word.rjust(max_length)}")
